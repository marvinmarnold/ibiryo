<li class="dropdown">
  <a href="#" class="dropdown-toggle"
								data-toggle="dropdown">
								<%= @shopper.email %>
								<b class="caret"></b>
	</a>
  <ul class="dropdown-menu">
    <li>
      <%= link_to t("navbar.account_links.account_settings"), edit_user_registration_path %>
    </li>
    <% if can? :update, Shop %>
      <li>
        <%= link_to t("navbar.account_links.manage_shops"), shops_path %>
      </li>
    <% end %>
    <% if can? :manage, MarketingStrategy %>
      <li>
        <%= link_to t("navbar.account_links.marketing_strategies"), marketing_strategies_path %>
      </li>
    <% end %>
<%
=begin
%>
    <li>
      <%= link_to t("navbar.account_links.history"), history_user_path(current_user) %>
    </li>
<%
=end
%>
    <li>
      <%= link_to t("navbar.account_links.sign_out"), destroy_user_session_path, :method => :delete %>
    </li>
  </ul>
</li>
